services:
  higi:
    build: .
    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./reports:/app/reports
      - ./config.yaml:/app/config.yaml:ro
    working_dir: /app
    entrypoint: ["tini", "--", "python", "main.py"]